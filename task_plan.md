# 任务计划：CertiEvidence 数据流水线重构方案

## 目标
形成一份可直接指导后续实现的完整数据处理方案，将多源 SWE 数据统一为防泄漏、可复现、可审计的 `release_v2`。

## 当前阶段
阶段 6（进行中）

## 各阶段

### 阶段 1：需求与发现
- [x] 理解当前项目的数据流、已有资产和质量问题
- [x] 调研可补充的 SWE 系列公开数据集
- [x] 明确用户接受多层数据金字塔方向
- [x] 将研究发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：总体数据设计
- [x] 定义数据源角色和接入优先级
- [x] 定义统一实体、可见性和标签分层
- [x] 定义跨数据集身份解析与防泄漏规则
- [x] 定义最终发布结构
- **状态：** complete

### 阶段 3：处理流水线规划
- [x] 拆分采集、规范化、注册、快照、语料、映射、标签、切分和发布阶段
- [x] 为每阶段定义输入、输出、失败策略和验收门槛
- [x] 规划现有数据向新流水线的迁移
- **状态：** complete

### 阶段 4：实现计划
- [x] 列出需要创建或修改的程序文件
- [x] 按 TDD 拆分可执行任务
- [x] 给出验证命令和预期结果
- **状态：** complete

### 阶段 5：自检与交付
- [x] 检查范围、术语、依赖和阶段一致性
- [x] 检查是否存在占位符或模糊验收条件
- [x] 更新 `progress.md`
- [x] 向用户交付方案文件
- **状态：** complete

### 阶段 6：Unified SWE Dataset 单脚本实现
- [x] 逐条读取统一数据集主设计文档
- [x] 核对仓库缓存、Tokenizer 和教师环境前提
- [x] 建立单脚本 TDD 实施计划
- [x] 实现 `scripts/build_unified_dataset.py` 的契约自测和 CLI
- [x] 实现来源校验、规范化、任务身份合并和冻结原始切分阶段
- [ ] 实现快照、语料、监督、候选、教师和发布阶段
- [ ] 生成 JSONL 实验版并通过完整审计
- [ ] 在教师配置可用后生成五文件 Parquet 正式版
- **状态：** in_progress

## 关键问题
1. 正式训练是否第一版只支持 Python？当前方案默认第一版以 Python 为主，schema 保留多语言扩展能力。
2. SWE-bench Verified、Lite 和其他公开评测集是否进入训练？当前方案默认只作评测，不进入训练。

## 已做决策
| 决策 | 理由 |
|------|------|
| 保留现有三类数据，并新增 Oracle/BM25、Agent 轨迹、SWE-rebench V2、SWE-Gym | 分别补足定位、行为、规模和可执行验证 |
| SWE-smith 只作为弱监督和预训练数据 | 合成任务规模大，但不能替代真实 Issue/PR Gold |
| 最终发布固定为 4 个数据文件和 `manifest.json` | 与统一数据集主设计一致：三个任务文件、唯一文件版本语料和发布元数据 |
| 所有 Gold/轨迹/补丁字段默认为离线标签 | 防止在线检索发生答案泄漏 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 全工作区 `git diff --stat` 扫描大 JSONL 时超时 | 1 | 自检改为只检查本轮新增规划文件，避免扫描用户的大数据改动 |
| 教师 API 环境变量全部缺失 | 1 | 继续实现并运行确定性阶段；正式发布在教师阶段硬失败，不生成降级数据集 |

## 备注
- 当前已进入实现阶段；来源阶段会在文件缺失时从冻结官方地址下载，并已重跑真实来源规范化和审计。
- 网页研究内容只写入 `findings.md`，不写入本文件。
