# Evidence Agent：项目创新点更新版

> **Gather, Combine, or Skip：面向软件修复上下文充分性的轨迹感知 Evidence Acquisition**

版本日期：2026-08-22

---

# 0. 论文定位先固定

本项目研究：

> **Software Repair Evidence Acquisition / Context Sufficiency**

而不是完整 Automated Program Repair。

在线输入：

```text
SWE Issue / Problem Statement
+
Pre-fix Repository Snapshot
```

在线输出：

```text
Structured Evidence Package
```

当前不做：

```text
Patch generation
Patch application
Test execution
Test-feedback repair loop
```

SWE-bench Gold Patch / Test Patch 只允许用于：

```text
offline supervision construction
reference-grounded evaluation
```

不能进入 online Retriever、Evidence Policy、Agent routing 或 Current Evidence State。

因此论文不需要证明：

> “Evidence Agent 自己能修复多少 bug。”

而需要证明：

> **Agent 能否在有限成本下，从修复前仓库中逐步获得足以理解并支持已验证参考修复所需的关键信息，并避免提前停止、无效探索和冗余上下文。**

---

# 一、核心研究问题

传统软件修复 Retriever 常见目标：

\[
q\rightarrow TopK(Code)
\]

或者学习：

\[
Score(q,u)
\]

其中候选代码 \(u\) 的价值只由 Issue \(q\) 决定。

但真实的多轮仓库探索具有状态性：

```text
已经知道目标函数以后，重复读取该函数的价值下降；
已经知道 buggy logic 以后，caller constraint 的价值上升；
单独的实现逻辑和单独的行为约束可能都不足，但二者联合足够；
已有一个等价约束证据后，另一个候选可能成为替代/冗余；
证据已经完整后，任何继续搜索都应被 STOP 压过。
```

因此本项目真正建模：

\[
(q,K_t)\rightarrow A_{t+1}
\]

其中：

- \(q\)：Issue；
- \(K_t\)：第 \(t\) 步已经获得的 Evidence；
- \(A_t\)：Single / Pair / STOP 动作。

策略为：

\[
A_t^*=\arg\max_A f_\theta(q,K_t,A)
\]

最终希望形成：

\[
K_0=\emptyset
\rightarrow K_1
\rightarrow \cdots
\rightarrow K_T
\]

使：

```text
关键 repair evidence obligations 被覆盖
+
不存在关键缺口
+
没有过度收集
+
STOP 时机正确
```

所以项目的研究对象不是简单的 Code Relevance，而是：

> **State-dependent Sequential Evidence Sufficiency Acquisition**

---

# 二、论文核心贡献建议：保持三项

为了避免“创新点很多但主线分散”，建议论文正式贡献保持三项：

```text
Contribution 1
Reference-Grounded Structured Evidence Sufficiency

Contribution 2
Trajectory-Aware State-Conditioned Evidence Policy

Contribution 3
Complementarity- and Substitutability-Aware Evidence Acquisition
```

Multi-stage Decision Boundary 是现在非常重要的新方法点，但建议先归入 Contribution 2。

如果最终 Stage 2 + rollout 的实验足够强，再把 Multi-stage Boundary 升级为独立第四贡献。

---

# 三、Contribution 1：Reference-Grounded Structured Evidence Sufficiency

## 3.1 从“找相关代码”转向“证据是否充分”

传统检索通常优化：

```text
Changed-file Recall
Changed-line Recall
Top-K relevance
Gold context overlap
```

这些指标回答：

> “相关位置有没有被找出来？”

但不能回答：

> “当前上下文是否已经足以理解为什么要这样修？”

本项目将目标改为：

> **Evidence Package 是否覆盖支持参考修复所需的关键信息角色。**

这一步将研究目标从：

```text
Localization correctness
```

提升为：

```text
Repair-context sufficiency
```

## 3.2 七类结构化 Evidence Obligations

项目使用 7-slot schema：

1. `fault_location`
2. `fault_logic`
3. `dependency_context`
4. `state_flow`
5. `behavior_constraint`
6. `repair_scope`
7. `validation_constraint`

这比：

```text
gold file
gold line
```

更加接近“支持合理修复所需要知道什么”。

例如，一个任务可能已经命中 faulty function，但仍然缺失：

```text
调用方约束
共享状态读写关系
修改是否影响其他分支
已有测试表达的行为要求
```

因此不能把 changed-location recall 直接等价为 context sufficiency。

## 3.3 OR-of-AND witness semantics

项目不是简单建立：

```text
obligation → one evidence
```

而是使用 OR-of-AND witness。

例如：

```text
[[2,5]]
```

表示：

\[
e_2\land e_5
\]

例如：

```text
[[2],[5,9]]
```

表示：

\[
e_2\lor(e_5\land e_9)
\]

因此：

```text
AND → complementary evidence
OR  → alternative / substitutable evidence path
```

这种逻辑结构比 flat binary relevance label 更适合修复上下文。

## 3.4 Reference-grounded，而不是 Gold-online

Gold Patch / Test Patch 只在 Agent 完成以后作为 evaluator reference。

评价问题是：

> Evidence 是否已经包含理解和支持该已验证参考修复所需的关键信息？

而不是：

> Evidence 是否泄漏了 patch 文本？

Gold Patch 也不是唯一可能正确修法，只是数据集中已经验证的 reference repair。

## 3.5 数学目标

可定义：

\[
S(K)\in[0,1]
\]

表示 Evidence Set 对关键 obligations / reference requirements 的覆盖程度。

最终优化：

\[
\max_\pi S(K_T)-\lambda Cost(K_T)
\]

其中 Cost 包括：

```text
Evidence Unit 数量
Evidence tokens
代码行数
Tool calls
Acquisition steps
```

## 3.6 为什么这一贡献有论文价值

它改变的是软件修复检索的**任务目标**，不是简单添加 feature。

传统问题：

```text
Which code is relevant?
```

本项目问题：

```text
What information is still missing for repair-context sufficiency?
```

正式贡献表述建议：

> **We formulate software-repair context acquisition as a reference-grounded structured evidence-sufficiency problem, where an agent must cover repair-relevant information obligations using only pre-fix repository evidence under a strict online/offline separation.**

中文：

> **提出面向参考修复约束的结构化证据充分性任务，将软件修复上下文获取从位置相关性优化提升为对故障位置、机理、依赖、状态、行为约束、修改范围和验证约束的联合覆盖。**

---

# 四、Contribution 2：Trajectory-Aware State-Conditioned Evidence Policy

这是项目最核心的算法与训练创新。

## 4.1 从静态 relevance 到条件边际价值

普通 reranker：

\[
s(u\mid q)
\]

本项目：

\[
s(A\mid q,K)
\]

模型不仅知道 Issue，还知道当前已经收集的 Evidence Set。

同一候选在不同 K 下可以有完全不同的价值。

定义边际充分性增益：

\[
\Delta(u\mid K)=S(K\cup\{u\})-S(K)
\]

进一步考虑成本：

\[
Q(u\mid K)=\Delta(u\mid K)-\lambda Cost(u)
\]

这使模型学习：

```text
what is useful now
```

而不是：

```text
what is globally relevant
```

## 4.2 Unified Single / Pair / STOP Policy

动作空间：

\[
A_t\in\{[u],[u,v],STOP\}
\]

统一 Cross-Encoder 同时评分：

```text
(q,K,[u])
(q,K,[u,v])
(q,K,STOP)
```

Single、Pair、STOP 不使用三个独立模型。

这样模型可以直接比较：

```text
获取一条新 Evidence
获取一组互补 Evidence
当前立即停止
```

## 4.3 Sufficiency-aware STOP

STOP 不是固定 Top-K 或固定轮数。

从 supervision 语义看：

```text
Incomplete state → STOP negative
Complete state   → STOP positive
```

因此可以自然定义：

\[
PrematureSTOPRate
\]

这对于真实 Agent 比单纯 ranking accuracy 更重要，因为一次提前 STOP 就会终止整个轨迹。

## 4.4 Stage 1 暴露的问题本身形成了研究动机

Stage 1 overall validation MRR 约：

```text
0.884
```

看起来已经不错。

但 Epoch 3 分状态：

```text
Initial MRR   ≈ 0.972
Complete MRR  ≈ 0.936
Boundary MRR  ≈ 0.630
```

同时：

```text
Boundary Hit@1       ≈ 27.52%
Boundary STOP Acc    ≈ 29.36%
```

这说明：

> **Aggregate metric 可以严重掩盖 Intermediate Decision failure。**

Initial 和 Complete 占据大多数 validation states，而且相对容易，因此 overall MRR 并不能证明模型会进行真实多步 Evidence Acquisition。

## 4.5 更进一步：原始 Boundary 与真实 Agent trajectory 不匹配

检查原始 builder 后发现，Boundary 基本来自：

```text
minimum sufficient certificate
− one evidence
```

也就是说它高度集中在：

```text
Near-complete state
```

真实 Agent 则会经历：

```text
Early
Mid
Late
Near-complete
```

因此：

> **Near-complete-only supervision 存在 trajectory state-distribution mismatch。**

这是项目当前最重要的方法学发现之一。

## 4.6 Multi-stage Decision Boundary Reconstruction

项目进一步从已有 Teacher / witness / corpus 中构造多阶段 Boundary，而不是重新收集数据。

Train：

```text
10,907 states
```

分布：

```text
Early          1,105  ≈10.1%
Mid            5,173  ≈47.4%
Late           3,901  ≈35.8%
Near-complete    728  ≈ 6.7%
```

Validation：

```text
200 states
```

分布：

```text
Early          22
Mid            95
Late           69
Near-complete  14
```

Audit：

```text
PASS
error_count = 0
```

这不是普通 oversampling。

`boundary-repeat=4` 只是：

```text
同一个 K 重复 4 次
```

Multi-stage reconstruction 则创建：

```text
不同阶段、不同 K、不同 remaining deficits
```

因此两者方法意义不同。

## 4.7 为什么 Multi-stage Boundary 应归入第二贡献

第二贡献研究的是：

> **如何让 Policy 对真实 sequential state 做正确决策。**

Multi-stage Boundary 正好解决训练分布缺失 Early/Mid/Late state 的问题，因此它不是独立的数据工程细节，而是 trajectory-aware policy learning 的监督设计。

正式表述建议：

> **We introduce a trajectory-aware state-conditioned evidence policy and reconstruct multi-stage decision boundaries spanning early, middle, late, and near-complete acquisition states, addressing the mismatch between near-complete supervision and actual sequential repository exploration.**

中文：

> **提出轨迹感知的状态条件化 Evidence Policy，并通过 Early/Mid/Late/Near-complete 多阶段决策边界监督，使模型学习真实证据获取轨迹中的中间决策和充分性停止，而不是仅拟合初始状态与近完成状态。**

---

# 五、Contribution 3：Complementarity- and Substitutability-Aware Evidence Acquisition

## 5.1 软件修复 Evidence 不是独立文档

传统 ranking 常隐含：

\[
Value(K)\approx\sum_{u\in K}Value(u)
\]

但真实修复上下文存在明显交互。

典型互补：

```text
buggy logic + caller constraint
state writer + state reader
exception source + handler
implementation + behavior test
目标函数 + 依赖函数语义
```

典型替代：

```text
完整函数 vs 对应关键 branch
caller 中明确约束 vs test assertion
多个重叠代码窗口
同一规则在代码/文档/测试中的重复表达
```

## 5.2 Complementarity

两条 Evidence 单独不充分，但联合产生新增覆盖。

可定义交互项：

\[
I(u,v\mid K)=
S(K\cup\{u,v\})
-S(K\cup\{u\})
-S(K\cup\{v\})
+S(K)
\]

当：

\[
I(u,v\mid K)>0
\]

说明存在正互补。

Pair action 的意义就是为这种结构提供显式动作。

## 5.3 Substitutability

如果候选 \(v\) 原本有价值：

\[
\Delta(v\mid K)>0
\]

但在加入 \(u\) 后：

\[
\Delta(v\mid K\cup\{u\})\approx 0
\]

则说明 \(u\) 与 \(v\) 存在信息替代关系。

因此策略应该避免：

```text
把多个表达同一信息的 Evidence 全部读入上下文
```

## 5.4 Redundancy

可定义：

\[
R(u\mid K)=\Delta(u\mid\emptyset)-\Delta(u\mid K)
\]

当 R 高时，表示候选可能总体相关，但对当前状态已经没有多少新增信息。

这解释了：

> **Relevant 并不等于 Useful-now。**

## 5.5 OR-of-AND 与 Single/Pair 的闭环

项目中 OR-of-AND witness 提供结构化监督：

```text
AND → complement
OR  → substitute / alternative path
```

Policy 动作空间提供：

```text
Single → 获取单项高价值证据
Pair   → 显式组合互补证据
STOP   → 当前无必要继续
```

从而形成：

```text
Representation
→ Supervision
→ Action space
→ Policy decision
```

完整闭环。

正式表述建议：

> **We model repair evidence as interacting rather than independent units, explicitly capturing complementary, substitutable, and redundant information through structured witnesses and unified single/pair/STOP decisions.**

中文：

> **显式建模软件修复证据之间的互补、替代和冗余关系，使 Agent 能够联合获取单独不足但组合有效的跨位置证据，同时避免重复读取信息功能等价的上下文。**

---

# 六、Multi-stage Boundary 是否应该作为独立第四创新？

当前建议：

> **先不独立，归入 Contribution 2。**

理由：

1. 它解决的是 state-conditioned policy 的训练分布问题；
2. 当前 Stage 2 最终 validation 尚未完成；
3. 论文贡献越多不代表越强，最好保持主线集中。

但如果最终出现以下完整证据：

```text
Stage1 best → Stage2 best
在同一 200-state validation 上显著提升
+
Initial / Complete retention 基本稳定
+
full rollout 中 premature STOP 显著下降
+
Sufficiency / Trajectory success 显著提高
```

则 Multi-stage Boundary 可以升级为独立第四贡献：

> **Multi-stage Decision Boundary Reconstruction for Trajectory-Aligned Evidence Policy Learning**

因为届时它已经不只是一个训练技巧，而是一个经过实验验证的：

```text
state-distribution diagnosis
→ reconstruction method
→ policy improvement
→ trajectory improvement
```

完整方法贡献。

---

# 七、Agent / Multi-Agent 层：不要过度包装

Agent 层应该服务于核心方法，而不是再堆很多创新。

推荐职责：

```text
Problem Analysis Agent
Retrieval Agent
Structure Exploration Agent
Evidence Policy Agent
Coordinator / Sufficiency Agent
```

核心可训练模型仍然只有：

```text
Evidence Policy
```

## 可选增强：State-Adaptive Repository Exploration

后续如果时间允许，可以研究：

> 根据当前 `q + K` 决定下一轮应该调用哪一种探索方式。

例如：

```text
K={} → global retrieval
已经定位函数 → canonical structure / caller
已知 logic 缺 behavior → test / constraint-oriented retrieval
已经充分 → STOP
```

这个点有价值，但建议作为：

```text
system-level enhancement
```

而不是第四/第五个核心理论创新。

如果没做，也不影响前三项主贡献成立。

---

# 八、当前已经形成的两个重要“实验发现”

这两点应该写进论文 Motivation / Analysis，而不只是放在实验表格里。

## 发现 1：Aggregate Metric 会掩盖 Intermediate State Failure

Stage 1：

```text
Overall MRR ≈ 0.884
```

但 Boundary：

```text
Hit@1 ≈ 27.52%
MRR   ≈ 0.630
STOP Accuracy ≈ 29.36%
```

因此：

> **只看 overall ranking 指标会高估 Agent 的 sequential acquisition ability。**

这支持论文必须分状态报告：

```text
Initial
Early
Mid
Late
Near-complete
Complete
```

而不是只有 overall MRR。

## 发现 2：Near-complete-only Boundary 不代表真实 trajectory

原始 Boundary 主要来自最小充分证书删除一条 Evidence，因此偏向 near-complete。

真实 Agent 却需要持续经历多个 incomplete prefixes。

所以：

> **State distribution 本身是 sequential Evidence Policy 学习中的关键设计变量。**

这使 Multi-stage reconstruction 的研究动机非常自然，而不是为了提高分数事后增加的数据增强。

---

# 九、对应实验设计：每个创新必须有直接证据

## 9.1 Contribution 1：Structured Sufficiency

需要证明：

```text
Gold location recall ≠ sufficiency
```

推荐分析：

```text
Location hit but obligation-incomplete cases
Obligation Coverage
Witness Coverage
Critical Deficit
Reference-grounded semantic sufficiency
```

还可以做：

```text
删除关键 witness → sufficiency下降
注入无关 evidence → redundancy上升但 sufficiency不应虚高
跨任务错配 → sufficiency显著下降
```

不依赖人工审查。

## 9.2 Contribution 2：Trajectory-aware State Policy

最关键：

```text
Stage1 best
vs
Stage2 best
```

必须在同一：

```text
200 Multi-stage Validation states
```

上比较。

分：

```text
Early
Mid
Late
Near-complete
```

报告：

```text
Hit@1
MRR
STOP Accuracy
Premature STOP
```

然后回测原始 501 validation 检查：

```text
Initial retention
Complete retention
```

最后 rollout 验证：

```text
Trajectory Success Rate
Sufficiency Success Rate
Premature STOP Rate
```

## 9.3 Contribution 3：Evidence Interaction

核心消融：

```text
Single-only
vs
Single + Pair
```

最好再按 witness 类型分组：

```text
single-witness task
multi-evidence AND witness task
alternative OR witness task
```

看 Pair 是否主要在 AND-complementary 场景中产生真实增益。

另外可报告：

```text
Redundant evidence rate
Over-collection
Evidence tokens
```

用于说明策略不仅会收集，还会避免替代/重复信息。

---

# 十、完整对比实验框架

项目同时包含 RAG、Policy、Agent，因此对比不能只放一个 baseline。

建议拆成三层。

## 10.1 Retrieval-level Baseline

```text
Path-only
Content/BM25-only
Path + Content RRF
RRF + Symbol
RRF + Canonical Structure
```

评价 candidate reachability。

## 10.2 Policy-level Baseline

```text
Static relevance scorer Score(q,u)
State-aware Score(q,K,u)
State-aware Single-only
State-aware Single + Pair + STOP
Stage1 near-complete-biased policy
Stage2 multi-stage policy
```

评价 state-level decision。

## 10.3 Agent-level Baseline

```text
Static one-shot Top-K RAG
Fixed-round iterative RAG
Iterative RAG + learned Policy
Full Multi-stage trained Evidence Agent
```

评价：

```text
Sufficiency
Premature STOP
Steps
Tokens
Over-collection
Trajectory success
```

这样可以清楚回答：

```text
RAG 带来什么
State-aware Policy 带来什么
Pair / STOP 带来什么
Multi-stage supervision 带来什么
完整 Agent rollout 带来什么
```

---

# 十一、论文结果的三条必要证据链

最终项目要达到高水平论文说服力，至少形成：

## Evidence Chain A：State-level Improvement

\[
Stage2 > Stage1
\]

在 same-200 Multi-stage Validation 上显著成立。

## Evidence Chain B：No Catastrophic Forgetting

Stage2 在：

```text
Initial
Complete
```

上没有明显退化。

## Evidence Chain C：Trajectory-level Improvement

从 \(K_0=\emptyset\) 开始 rollout，Stage2 / final model 能：

```text
提高 sufficiency success
降低 premature STOP
提高 trajectory success
控制 evidence count / token cost
减少 over-collection
```

只有三条同时成立，才能证明 Multi-stage state learning 真正改善了 Agent，而不是只提高一个离线边界分类分数。

---

# 十二、当前项目事实与“不能提前声称”的结果

## 已经可以写成事实

```text
20,864 tasks
20,859 experiment eligible
25,496,300 Evidence Units
1,027,752 file versions
20,588 Teacher rows
7-slot obligations
OR-of-AND witness semantics
Single / Pair / STOP action space
Stage1 completed
Stage1 aggregate-vs-boundary gap observed
Multi-stage Boundary reconstructed
Train = 10,907
Validation = 200
Audit PASS / 0 error
Stage2 已进入 Epoch 2/2
```

## 目前不能写成最终结论

```text
“Stage2 Boundary 泛化已经达到 85%+”
“Multi-stage 一定显著优于 Stage1”
“Agent trajectory 已经成功”
“Premature STOP 已经解决”
“最终 benchmark 已提高”
```

因为这些还需要：

```text
Stage2 validation
same-200 comparison
retention
benchmark
rollout
```

---

# 十三、与普通 RAG / Code Reranker 的区别

如果审稿人问：

> 这是不是只是一个代码 reranker？

可以回答：

普通 reranker：

\[
Score(q,u)
\]

通常输出一次 Top-K。

本项目：

\[
Score(q,K_t,A_t)
\]

其中：

```text
K 会随动作变化；
A 可以是 Single、Pair、STOP；
Evidence 具有 OR-of-AND interaction；
Policy 训练覆盖多阶段 incomplete states；
最终评价完整 trajectory，而不是一次 ranking。
```

因此核心差异是：

```text
Static relevance ranking
→
Sequential state-conditioned evidence policy
```

---

# 十四、与自动程序修复系统的区别

如果审稿人问：

> 为什么不直接比较 SWE-bench patch resolve rate？

回答应是：

当前项目研究对象是：

```text
Evidence Acquisition
```

不是：

```text
Patch Generation
```

Patch resolve rate 会同时混入：

```text
Evidence quality
Generator能力
Patch decoding
Search strategy
Test execution
Repair loop
```

这样很难单独研究 Evidence acquisition 本身。

因此项目首先隔离并研究：

> Agent 是否能获得足以支持合理修复的上下文。

Gold Patch / Test Patch 仍然保留为 evaluator-only reference，因此评价并没有脱离真实修复任务。

---

# 十五、论文标题建议

当前最推荐：

## Gather, Combine, or Skip: Trajectory-Aware Evidence Acquisition for Software Repair

它对应：

```text
Gather  → 获取当前最有增量价值的 Evidence
Combine → 组合互补 Evidence
Skip    → 跳过替代/冗余 Evidence，并在充分时 STOP
```

如果 Multi-stage Boundary 最终成为最强实验点，可以考虑：

## Beyond Near-Complete Context: Multi-Stage Evidence Policy Learning for Software Repair

如果想强调 sufficiency：

## Structured Evidence Sufficiency for Agentic Software Repair Context Acquisition

如果想强调 interaction：

## Interaction-Aware Sequential Evidence Acquisition for Software Repair

当前最平衡的仍是第一项。

---

# 十六、论文摘要级中文表述

> 现有软件仓库检索方法通常独立评估 Issue 与代码片段的相关性，难以刻画候选证据在当前已知上下文下的条件价值，也无法自然处理修复证据之间的互补、替代和停止关系。本文将软件修复上下文获取建模为严格 pre-fix 条件下的序列证据充分性问题。我们首先以七类 repair evidence obligations 和 OR-of-AND witnesses 表示修复上下文的信息需求及其替代/互补结构；随后提出状态条件化 Evidence Policy，在统一动作空间中对单证据、双证据和 STOP 进行排序。针对基础训练中 aggregate metric 掩盖中间状态失败、且 near-complete-only supervision 与真实 Agent trajectory 不匹配的问题，我们进一步构造 Early、Mid、Late 与 Near-complete 多阶段 Decision Boundary，用于 trajectory-aware policy learning。最终系统从空 Evidence State 开始多轮获取，并以 obligation/witness coverage、premature STOP、trajectory success、上下文成本及 reference-grounded semantic sufficiency 评价 Agent 是否能够获得充分而不过量的修复证据。系统输出 Evidence Package，而非代码补丁；Gold Patch 与 Test Patch 仅作为离线监督或评价参考，不进入在线 Agent。

---

# 十七、论文摘要级英文表述

> Existing repository retrieval methods typically rank code snippets independently with respect to an issue, overlooking that the utility of a candidate depends on what the agent has already observed and that repair evidence can be complementary, substitutable, or redundant. We formulate software-repair context acquisition as a sequential evidence-sufficiency problem under a strict pre-fix setting. Repair information needs are represented by structured obligations and OR-of-AND witnesses, which capture alternative and jointly required evidence paths. We then learn a state-conditioned cross-encoder policy over unified single, paired, and STOP actions. Our Stage-1 analysis reveals that aggregate ranking metrics can mask severe failures at intermediate acquisition states and that near-complete-only decision-boundary supervision does not match actual sequential trajectories. To address this mismatch, we reconstruct multi-stage decision boundaries spanning early, middle, late, and near-complete states for trajectory-aware policy learning. The final agent is evaluated from an empty evidence state using sufficiency success, premature stopping, trajectory success, acquisition cost, over-collection, and reference-grounded semantic sufficiency. The system produces an evidence package rather than a patch; verified patches and test patches are used only for offline supervision or evaluation and are never exposed to the online agent.

---

# 十八、最终建议：论文创新怎么写最稳

建议主论文 Contributions 段落最终保持：

```text
1. Structured Evidence Sufficiency
2. Trajectory-Aware State-Conditioned Policy
3. Evidence Interaction-Aware Acquisition
```

其中：

```text
Contribution 1
= 任务定义 + 7-slot obligation + OR-of-AND sufficiency

Contribution 2
= q+K+A policy + STOP + Multi-stage Decision Boundary

Contribution 3
= complement / substitute / redundancy + Pair action
```

Agent / Multi-Agent：

```text
属于系统执行层
```

Reference-grounded Judge：

```text
属于评价层
```

Retriever / Canonical Structure：

```text
属于候选生成与可达性层
```

这样论文不会变成：

> “我们做了很多模块。”

而会形成一条清晰主线：

```text
定义什么叫充分
↓
学习当前状态下一步应该获取什么
↓
建模多证据之间如何联合/替代
↓
修正真实 trajectory 的训练状态分布
↓
从 K={} 验证整个 Agent 是否真的完成证据获取
```

这也是当前项目最有希望形成高质量论文叙事的结构。
