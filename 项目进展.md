# 软件修复证据获取项目阶段总结与后续执行路线

## 一、项目当前处于什么阶段

项目已经完成了三类基础工作：

1. **统一软件修复任务和多源标签的数据工程基础**；
2. **候选级证据数据集 MVP、认证标签及质量审计**；
3. **严格按仓库版本隔离的完整仓库语料构建器设计与部分运行**。

但目前还没有进入正式模型训练阶段。

当前最重要的现实状态是：

```text
完整仓库 inventory 曾经成功完成
↓
19,311 个 snapshot
28,099,838 条 snapshot-file 归属关系
1,255,421 个唯一 extraction
↓
随后当前 build_state.sqlite3 中 inventory 关系丢失
↓
最新状态重新变成：
snapshot_count = 19,311
snapshot_file_membership_count = 0
unique_extraction_count = 0
```

因此，当前不是“extract 已经完成”，而是需要先重新恢复完整仓库 inventory，再运行加速后的 extract。

从整个研究路线看，目前大约完成了：

```text
任务定义与数据工程：较完整
候选级 MVP：已完成，但质量不足
完整仓库语料：程序完成，运行状态需要恢复
多源支持证据统一：尚未正式实现
检索索引：尚未实现
状态训练样本：尚未实现
模型训练：尚未开始
行为价值监督：尚未开始
最终证书评测：尚未完成
```

---

# 二、项目最终研究目标已经明确

项目研究的问题不是生成补丁，而是：

> 在有限预算下，软件修复系统应该读取哪些修复前仓库证据，哪些证据必须组合，哪些证据可以相互替代，以及何时停止继续探索。

当前理论路线已经包含三个相互衔接的层次。

## 2.1 数据与证据层

需要从修复前仓库中获取：

```text
故障位置
故障逻辑
依赖上下文
状态流
行为约束
修改范围
相关测试
调用方或被调用方
```

这些证据都必须来自：

```text
repo + base_commit
```

对应的修复前快照。

Gold patch、test patch、ContextBench gold context、SWE-Explore trajectory 等只能作为离线标签，不能暴露给在线检索模型。项目执行方案明确要求所有模型可见代码都来自修复前 `base_commit`，外部 gold context 和 patch 只能用于监督。

## 2.2 策略层

模型不是简单预测“某条代码是否相关”，而是估计当前证据状态下某个候选的条件价值：

[
V_G(K)=P_G(\text{repair succeeds}\mid q,K)
]

[
\Delta_G(u\mid K)=V_G(K\cup{u})-V_G(K)
]

[
Q_G(u\mid K)=\Delta_G(u\mid K)-\lambda Cost(u)
]

其中：

* (q)：问题描述；
* (K)：当前已获取证据；
* (u)：候选证据；
* (G)：冻结的下游修复器；
* (V_G(K))：证据包对下游修复器的价值；
* (\Delta_G(u\mid K))：候选证据的条件边际价值；
* (Q_G(u\mid K))：扣除读取成本后的净价值。

这套定义用于解释：

```text
为什么现在应该读取某条证据
为什么同一条证据在不同状态下价值不同
为什么有些证据必须组合
为什么有些证据可以替代
为什么系统此时应该停止
```

## 2.3 认证层

策略模型负责选择证据，但不能单独决定证据已经充分。

最终需要一个机器可验证层：

```text
候选选择模型
+
证据义务
+
witness groups
+
程序结构路径
+
风险阈值
=
CERTIFIED / EXPAND / ABSTAIN
```

建议最终将两个研究表述统一：

* **VOI Policy**：回答“下一步读取什么”；
* **Evidence Certificate**：回答“当前证据是否足以停止”。

这样既保留交互感知和价值学习的算法主线，也保留可认证证据获取的安全边界。

---

# 三、已经完成的数据工程工作

## 3.1 原始数据规范化

已经完成用于统一不同来源数据结构的脚本：

```text
01_normalize_local_sources.py
```

主要工作：

* 统一 SWE-bench、ContextBench、SWE-Explore 等来源；
* 规范 repo、commit、问题描述和数据来源字段；
* 保留 source-specific annotation；
* 将 patch、test patch、trajectory、gold context 标记为离线资产；
* 防止标签进入在线模型字段。

这一阶段解决的是：

> 不同数据来源字段不一致，无法直接合并的问题。

---

## 3.2 Master Instance Registry

已经完成：

```text
02_build_master_registry.py
```

作用是将不同数据集中可能重复的任务归并成统一实例。

统一身份大致依据：

```text
repo
+ base_commit
+ issue / PR / instance identity
+ normalized problem statement
```

已经建立：

```text
canonical_instance_id
task_group_id
snapshot_id
source membership
```

这样同一个软件问题不会因为出现在多个数据集中而被当成多个独立训练样本。

这也是后续避免以下泄漏的基础：

```text
同一 issue 同时进入 train 和 test
同一 PR 的不同表示进入不同 split
同一仓库版本的近重复任务跨 split
```

---

## 3.3 数据集划分

已经完成：

```text
03_freeze_splits.py
```

当前 MVP 的划分规模为：

```text
train              14,859
dev                 2,259
test_retrieval      2,350
test_sufficiency    2,514
```

总计：

```text
21,982 个发布任务
```

当前 manifest 显示不存在重复实例 ID、标签缺失单元和单实例多 snapshot 等基本完整性问题。

但后续正式实验仍需要进一步加强：

```text
repository-disjoint split
task-group-disjoint split
issue/PR-disjoint split
snapshot-disjoint split
```

尤其需要一个严格的 held-out repository test。

---

## 3.4 Git 快照准备

已经完成：

```text
04_prepare_git_snapshots.py
```

它负责：

* 根据 repo 和 base commit 准备修复前 Git 快照；
* 解析完整 commit；
* 建立 `snapshot_id`；
* 确保每个 snapshot 唯一绑定：

```text
repo + resolved_commit
```

这一步建立了后续最关键的隔离规则：

> 每个问题只能检索自己对应的仓库版本。

不能只根据 repo 过滤，因为同一仓库不同 commit 的代码不能混用。

---

## 3.5 证据 anchor 抽取

已经完成：

```text
05_extract_evidence_anchors.py
```

它主要从离线标签中抽取：

```text
patch old-side
test patch
ContextBench gold spans
SWE-Explore core / optional regions
trajectory read regions
其他 external context
```

这些 anchor 用来对齐修复前代码，而不是直接作为在线输入。

---

## 3.6 修复前 Evidence Unit 抽取

已经完成：

```text
06_extract_pre_fix_evidence_units.py
```

并使用 SQLite 做断点状态：

```text
data/processed/evidence_units/extraction_state.sqlite3
```

这一阶段从与 gold 标签对齐的文件和区域中抽取：

```text
函数
类
方法
局部代码块
文件窗口
相关结构单元
```

这些单元形成了第一个候选级证据池。

---

## 3.7 自动证据义务与认证标签

已经完成：

```text
07_build_certification_labels.py
```

自动生成的义务包括：

```text
fault_location
fault_logic
dependency_context
state_flow
behavior_constraint
repair_scope
```

并构造：

```text
repair obligations
obligation witnesses
reference certificates
```

当前结果：

```text
处理实例：21,982
certified：21,979
uncertified：3
认证率：99.9864%
witness sets：191,600
reference certificate units：26,144
平均证书大小：1.189
```

但这些结果后来被审计证明过于容易，不能直接作为正式充分性真值。

---

## 3.8 MVP 发布数据集

已经完成：

```text
08_build_release_dataset.py
```

当前发布结构：

```text
data/release/
├── benchmark.jsonl
├── evidence_corpus.parquet
└── manifest.json
```

其中：

```text
benchmark.jsonl
    21,982 条任务

evidence_corpus.parquet
    291,747 个 Evidence Units
    254,256 条结构边
    合计 546,003 条记录
```

当前 MVP 的重要优点：

* 任务 ID 唯一；
* snapshot 归属明确；
* 在线字段与离线标签分离；
* patch、test patch、trajectory、gold context 被明确禁止进入在线 Agent；
* 基本 split 和引用完整性检查通过。

当前 manifest 也明确声明：此 corpus 是 `aligned_candidate_corpus`，只能用于候选级证据获取和初步充分性实验，不能表述为完整仓库无偏检索语料。

---

# 四、已经完成的质量审计

已经编写：

```text
10_audit_release_quality.py
```

审计输出：

```text
results/data_quality_audit/
├── quality_report.json
├── quality_report.md
└── problem_instances.jsonl
```

主要发现如下。

## 4.1 候选与标签严重塌缩

当前数据中：

```text
witness union ≈ candidate set
```

大量任务中，几乎所有候选都被某种义务标记为 witness。

结果是：

```text
缺少真正负例
Retriever 排序任务过于容易
无法训练有效的证据选择策略
```

---

## 4.2 fault_location 范围过宽

很多任务的 `fault_location` 覆盖了几乎全部候选。

因此模型可能只需要识别任务级候选池，不需要真正定位故障。

---

## 4.3 证书过于简单

当前：

```text
single-unit certificate rate = 83.8641%
```

也就是超过八成任务只需要一个 Evidence Unit 就能“认证”。

manifest 已主动警告：自动监督认证率超过 99%，且超过 80% 的参考证书只有一个单元，必须强化 dependency、state 和 constraint 义务。

这与软件修复需要跨位置证据的研究动机不一致。

---

## 4.4 dependency_context 太弱

当前 `dependency_context` 的覆盖数量很低，且主要依赖保守代理。

缺少可靠的：

```text
call edge
import edge
def-use edge
read/write relation
exception propagation
override relation
```

---

## 4.5 state_flow 标签噪声较大

当前 state_flow 曾使用共享标识符作为代理。

常见标识符、注释、docstring 或普通变量可能造成误关联。

这不能用于正式的状态传播证明。

---

## 4.6 repair_scope 和 state_flow 的 mandatory 设计不足

在部分版本中：

```text
state_flow
repair_scope
```

几乎从不成为真正的 mandatory obligation。

因此它们无法迫使证书包含跨文件或跨状态证据。

---

## 4.7 当前 corpus 不是完整仓库语料

约 75% 的任务有独立 snapshot，当前 corpus 仍然主要是 gold 派生的任务级候选集合。

它缺少：

```text
同文件硬负例
同模块硬负例
同名函数干扰项
仓库内无关代码
同仓库真实背景代码
```

因此不能用于 FullRepo 检索实验。

---

## 4.8 结论

当前 release 必须冻结为：

```text
MVP / debug dataset
```

它可以用于：

* 数据结构验证；
* 运行流程验证；
* 代码接口开发；
* 候选级 sanity check。

不能用于：

* 正式主实验；
* 证明证据充分性；
* 证明最小证书；
* FullRepo 检索；
* 论文最终性能结论。

---

# 五、ContextBench、SWE-Explore 和 SWE-bench 的统一设计已经明确

已经完成概念层面的统一方案。

## 5.1 SWE-bench 的作用

提供：

```text
problem_statement
repo
base_commit
patch
test_patch
FAIL_TO_PASS
PASS_TO_PASS
```

主要用于：

* 任务定义；
* 修复前快照身份；
* patch old-side 故障定位；
* 测试约束候选；
* 固定修复器行为评测。

---

## 5.2 ContextBench 的作用

提供人工标注的：

```text
文件
起止行
gold context
相关 span
```

主要用于：

* 人工相关上下文监督；
* span-level retrieval；
* 高质量 supporting evidence；
* 参考证据全集的重要组成部分。

ContextBench 的 gold context 已经包含文件路径、行区间和内容，因此不需要再让 LLM 从 patch 猜测完整上下文。

---

## 5.3 SWE-Explore 的作用

提供：

```text
core_context
optional_context
successful trajectories
read-step provenance
读取顺序
```

主要用于：

* 多跳证据获取顺序；
* 成功路径读取集合；
* 替代证据候选；
* 多组充分证书候选；
* trajectory-grounded exploration。

重要结论是：

```text
trajectory 读取过
≠
证据必需
```

所以 core 和 optional 不能直接整体当成充分证据。

---

## 5.4 两个数据集的互补关系

ContextBench 回答：

> 人工认为应该看什么？

SWE-Explore 回答：

> 成功解决过程实际看了什么？

组合后可以区分：

```text
人工 gold ∩ trajectory core
    最高置信度支持证据

人工 gold ∩ trajectory optional
    替代路径或辅助证据

人工 gold only
    人工认为相关，但成功轨迹不稳定依赖

trajectory core only
    可能是人工遗漏，也可能是 Agent 行为偏差

trajectory optional only
    弱证据、替代候选或探索噪声
```

---

## 5.5 已确定不能简单合并成二分类标签

不能直接做：

```text
ContextBench gold
∪ SWE-Explore core
∪ SWE-Explore optional
∪ patch units
=
positive
```

必须保留来源和角色：

```json
{
  "unit_id": "...",
  "annotations": [
    {
      "source": "contextbench",
      "role": "human_gold"
    },
    {
      "source": "swe_explore",
      "role": "trajectory_core"
    },
    {
      "source": "swe_bench",
      "role": "patch_old_overlap"
    }
  ]
}
```

统一支持等级建议为：

```text
strong_support
support
weak_support
unlabeled_candidate
```

---

# 六、支持、充分和完整证据的概念已经区分

## 6.1 支持证据

支持证据是能够见证某条证据义务的单元或单元组合。

例如：

```text
故障函数
调用方
被调用依赖
共享状态定义
测试断言
异常处理器
```

支持证据必须记录：

```text
来源
证据角色
对应 obligation
结构关系
置信度
是否人工标注
是否轨迹支持
是否 patch 对齐
```

---

## 6.2 完整证据

不能宣称获得了仓库中“绝对完整”的所有相关证据。

更准确的名称是：

```text
reference_evidence_universe
```

其定义是：

> 相对于当前可观测数据源、程序分析和审核结果得到的参考证据全集。

来源包括：

```text
ContextBench human gold
SWE-Explore core
SWE-Explore optional
patch old-side
test patch
successful trajectory reads
static structural closure
有限语义审核
```

必须记录：

```text
completeness_status
source coverage
unmapped spans
unresolved relations
```

建议状态：

```text
strong_reference
partial_reference
unknown
```

---

## 6.3 充分证据

充分证据集合 (S) 必须满足：

[
S\subseteq R_x
]

其中 (R_x) 是参考证据全集，并且：

[
\forall o\in O_x^{mandatory},
\quad Covered(o,S)=1
]

还要满足：

* 所有 `all_required` witness 成员齐全；
* 所有 ordered structural path 真实存在；
* 所有证据属于同一 snapshot；
* 不存在 unresolved mandatory obligation。

---

## 6.4 最小充分证据

已经区分两种最小性。

### 包含极小

删除任何一个单元后都不再充分：

[
\forall u\in S,
\quad
S\setminus{u}
\text{ 不充分}
]

### 成本最小

在所有充分集合中成本最低：

[
S^*=
\arg\min_{S:\text{Sufficient}(S)}
Cost(S)
]

成本可以包括：

```text
Evidence Unit 数
代码行数
token 数
读取次数
工具调用数
总推理成本
```

---

## 6.5 多组充分证书

同一个任务可能存在：

```text
S1 = {故障函数, 状态定义, 调用方}
S2 = {故障函数, 状态定义, 相关测试}
S3 = {故障函数, 被调用依赖, 接口约束}
```

因此不能只保存一组 gold certificate。

建议保存：

```json
{
  "minimal_sufficient_certificates": {
    "sets": [],
    "enumeration_complete": true,
    "truncated": false
  }
}
```

若只枚举前 64 组，必须记录：

```text
enumeration_complete = false
truncated = true
```

---

# 七、Nips 项目已经完成完整机制阅读和迁移分析

已经阅读该项目的核心：

```text
数据构建
PrefixRankingDataset
CrossEncoderRanker
训练损失
在线检索
BM25/Dense/RRF
局部扩展
MMR
Full State
Query-only
Anchor
ECDR-inspired
teacher-student 构建
停止和回答流程
```

结论如下。

## 7.1 Nips 不输入完整语料

它的模型输入是：

```text
Question + K_t
+
单个候选 Evidence Unit
```

Dataset 会把同一个 `context_text` 与每个候选逐对展开。

官方最大长度只有 320 tokens，进一步证明模型没有读取整个 Wikipedia。

## 7.2 Nips 的完整语料只是候选来源

其训练流程大致是：

```text
问题级 memory pool
→ BM25 top-30
→ Dense top-30
→ RRF
→ 同文档局部扩展
→ MMR
→ 10 个候选
→ DeBERTa Cross-Encoder
```

模型本身只对压缩后的候选池排序。

## 7.3 对我们项目的迁移

对应修改：

```text
memory_by_qid
→
memory_by_snapshot
```

局部扩展从：

```text
同文档前后句
```

改为：

```text
同文件邻居
父函数/父类
caller/callee
imports
read/write
def-use
override
测试调用
异常传播
```

## 7.4 不能直接照搬的部分

Nips 训练时会强制把正例插入候选池。

我们的规则应为：

```text
Ranker 训练：
允许 force-positive，但必须记录

FullRepo 评测：
禁止 force-positive
未召回就是 Retriever 失败
```

Nips 使用单一 teacher sequence，而我们的任务可能有多组充分证书。因此训练正例必须是集合值：

```text
acceptable_next_unit_ids
```

不能只有一个 `positive_unit_id`。

---

# 八、完整仓库语料构建器已经完成

已经编写并多次修复：

```text
11_build_full_repository_corpus.py
```

其目标是构建严格按 Git 快照隔离的完整可检索仓库语料。

核心原则包括：

1. 每个问题只能检索自己的 `snapshot_id`；
2. `snapshot_id` 唯一绑定 repo 和 commit；
3. gold 标签不参与语料文件选择；
4. 完整仓库文件独立于标签构建；
5. 相同 Git blob 物理去重；
6. 逻辑 Evidence Unit 保留 snapshot 和路径身份；
7. 支持断点续跑；
8. 最终生成一个 `repository_corpus.parquet`。

---

## 8.1 Parquet 双记录设计

一个物理文件包含两类记录。

### content_unit

保存唯一代码内容：

```text
content
content_sha256
blob_oid
unit_type
start_line
end_line
```

相同 blob 只保存一次内容。

### snapshot_unit

保存版本归属：

```text
snapshot_id
repo
resolved_commit
file_path
logical_unit_id
content_unit_id
```

这样可以：

```text
物理去重代码内容
+
逻辑保持仓库版本隔离
```

---

## 8.2 文件覆盖策略

语料包括：

```text
源代码
测试代码
配置
构建脚本
schema
模板
必要文档
```

排除：

```text
二进制
依赖缓存
node_modules
venv
vendor
third_party
编译产物
图片
字体
压缩文件
过大文件
```

固定行块保证所有纳入文本都可检索；Python AST 和其他语言符号窗口用于增加细粒度候选。当前默认参数为：

```text
chunk_lines = 120
chunk_overlap = 20
max_file_bytes = 2 MB
cat-file batch = 1024
prefetch batch = 2000
```

---

# 九、完整仓库构建过程中已经解决的问题

## 9.1 inventory 卡在第一个仓库

原始命令使用：

```text
git ls-tree -r -l -z
```

其中 `-l` 会查询 blob 大小。

在 `--filter=blob:none` 的 partial clone 中，这会触发缺失 blob lazy fetch，导致 inventory 实际开始下载整个仓库。

已经修复为：

```text
GIT_NO_LAZY_FETCH=1
git ls-tree -r -z
```

文件大小延迟到 extract 阶段读取真实 blob 后再检查。

---

## 9.2 inventory 成功结果

曾经成功得到：

```text
snapshot_count                  19,311
snapshot_file_membership_count  28,099,838
unique_extraction_count          1,255,421
unique_content_unit_count                0
```

`content_unit_count = 0` 当时是正常的，因为只完成了 tree inventory。

---

## 9.3 extract 初版太慢

初版出现：

```text
DataDog/integrations-core
29,548 unique extractions
每次只处理 64 个
```

主要问题：

1. 每 64 个 blob 启动一次 `git cat-file`；
2. partial clone 产生大量小粒度 lazy fetch；
3. 每个 extraction 回写 `snapshot_files`；
4. 总计可能改写约 2,809 万条成员记录。

---

## 9.4 extract 加速版

已经改为：

```text
离线检测缺失 blob
→ git fetch --stdin 批量预取
→ 生成 Git pack
→ GIT_NO_LAZY_FETCH=1 本地 cat-file
→ 批量 1024 个解析
→ 只更新 extractions/content_units
```

并取消每个 extraction 对 `snapshot_files` 的回写放大。

已经通过本地 partial-clone bulk prefetch 测试。

---

## 9.5 当前状态库丢失

最新运行出现：

```text
repo_count = 0
snapshot_file_membership_count = 0
unique_extraction_count = 0
```

说明：

```text
snapshots 表重新注册了 19,311 条
但 snapshot_files 和 extractions 已为空
```

这通常意味着：

* 状态数据库被删除或覆盖；
* 曾运行 `--reset-state`；
* 切换过输出目录；
* 复制脚本或目录时替换了状态文件；
* SQLite 状态未完整保留。

已经加入 extract 前置条件检查：

```text
successful_snapshot_count > 0
snapshot_file_count > 0
extraction_count > 0
```

为空时直接返回错误，不再误报 `passed`。

---

# 十、当前已有的主要程序和资产

## 10.1 数据构建脚本

```text
01_normalize_local_sources.py
02_build_master_registry.py
03_freeze_splits.py
04_prepare_git_snapshots.py
05_extract_evidence_anchors.py
06_extract_pre_fix_evidence_units.py
07_build_certification_labels.py
08_build_release_dataset.py
10_audit_release_quality.py
11_build_full_repository_corpus.py
```

## 10.2 当前数据资产

```text
data/registry/master_instances.jsonl
data/registry/git_snapshots.jsonl
data/splits/split_assignments.jsonl
data/processed/evidence_units/extraction_state.sqlite3
data/processed/certification/certification_labels.jsonl
data/release/benchmark.jsonl
data/release/evidence_corpus.parquet
data/release/manifest.json
```

## 10.3 当前完整仓库目标资产

```text
data/processed/full_repository_corpus/
├── repository_corpus.parquet
├── build_state.sqlite3
├── corpus_report.json
└── corpus_failures.jsonl
```

目前这一目标资产尚未真正构建完成。

---

# 十一、接下来必须先做的工作：恢复完整仓库语料

这是当前最高优先级。

## 11.1 替换最新脚本

使用包含：

```text
严格离线 inventory
bulk prefetch extract
1024 批量
取消 membership 写放大
空 inventory 防护
```

的最新脚本。

## 11.2 重新运行 inventory

不能加：

```text
--reset-state
```

运行：

```powershell
python scripts/11_build_full_repository_corpus.py `
  --project-root . `
  --phase inventory `
  --inventory-timeout 30
```

## 11.3 验证状态

运行：

```powershell
python scripts/11_build_full_repository_corpus.py `
  --project-root . `
  --phase status
```

验收条件：

```text
successful snapshot 接近 19,311
snapshot_file_count 接近 28,099,838
unique extraction 接近 1,255,421
content_unit_count = 0
```

## 11.4 运行加速 extract

```powershell
python scripts/11_build_full_repository_corpus.py `
  --project-root . `
  --phase extract `
  --prefetch-mode auto `
  --prefetch-batch-size 2000 `
  --prefetch-timeout 900 `
  --batch-size 1024 `
  --git-timeout 300 `
  --retries 2 `
  --max-attempts 4 `
  --progress-every 1024
```

## 11.5 extract 验收

需要确认：

```text
extraction success 数量持续增长
content_unit_count 持续增长
failed 比例可解释
没有大规模 git_blob_missing
没有跨 snapshot 污染
中断后可续跑
```

## 11.6 导出 Parquet

extract 完成后：

```powershell
python scripts/11_build_full_repository_corpus.py `
  --project-root . `
  --phase export `
  --overwrite-export
```

## 11.7 完整仓库语料验收

必须检查：

```text
每个 benchmark snapshot 都存在
每个 snapshot 至少有一个可检索单元
content_unit 引用完整
logical_unit_id 无重复
没有 unit 跨 snapshot
repo+commit 唯一映射
所有内容来自 pre-fix commit
没有 patch-added code
没有 forbidden online fields
```

---

# 十二、完整仓库完成后的下一阶段：多源证据统一

建议下一脚本：

```text
12_merge_multisource_reference_evidence.py
```

输入：

```text
master_instances
ContextBench gold context
SWE-Explore core/optional/trajectory
SWE-bench patch/test patch
repository_corpus.parquet
```

输出：

```text
data/processed/reference_evidence/
└── reference_evidence.jsonl
```

每条记录至少包含：

```json
{
  "canonical_instance_id": "...",
  "snapshot_id": "...",

  "source_annotations": {
    "contextbench": {},
    "swe_explore": {},
    "swe_bench": {}
  },

  "supporting_units": [],
  "reference_evidence_universe": {},
  "mapping_report": {},
  "uncertainty": {}
}
```

## 12.1 Span 映射

需要把所有外部范围映射到：

```text
logical_unit_id
```

映射规则包括：

```text
精确 file path
精确 snapshot
行区间 overlap
最小包含单元
内容哈希校验
模糊路径修复
重命名处理
```

## 12.2 保留 source-specific 标签

不能提前压缩为一个 boolean。

必须保留：

```text
human_gold
trajectory_core
trajectory_optional
patch_old_overlap
test_patch_overlap
static_closure
```

## 12.3 保留每条 SWE-Explore 轨迹

不能只保留 core 和 optional 的聚合结果。

需要保存：

```text
trajectory_id
ordered_unit_ids
read positions
successful run identity
```

否则无法恢复多条成功路径，也无法构造多组充分证书候选。

---

# 十三、程序结构图需要重新构建

建议新增：

```text
13_build_snapshot_structure_graph.py
```

每个结构边必须属于同一个 snapshot。

至少需要：

```text
contains
defines
imports
calls
called_by
reads
writes
raises
handles
overrides
implements
same_symbol
test_calls
span_overlap
```

后续证据组合不能仅靠共享词语。

## 13.1 Python

可使用：

```text
ast
import resolution
qualified symbol table
call approximation
attribute read/write
exception relation
```

## 13.2 其他语言

第一版可以使用：

```text
tree-sitter
语言级声明解析
import/package 解析
启发式 call relation
```

## 13.3 结构图用途

用于：

* 局部候选扩展；
* dependency_context witness；
* state_flow witness；
* repair_scope；
* 多跳路径验证；
* 互补证据候选；
* 最终证书验证。

---

# 十四、支持证据标签需要正式构建

建议新增：

```text
14_build_supporting_evidence_labels.py
```

统一支持等级：

```text
strong_support
support
weak_support
unresolved
unlabeled_candidate
```

示例规则：

### strong_support

```text
ContextBench human gold
+
SWE-Explore core

或

ContextBench human gold
+
patch old-side 精确重叠
```

### support

```text
ContextBench gold only
SWE-Explore core only
patch old-side 精确单元
```

### weak_support

```text
SWE-Explore optional
单条成功轨迹读取
静态结构闭包
LLM 弱审核
```

### unresolved

```text
多来源冲突
span 无法精确对齐
轨迹读取但无结构或人工支持
```

完整仓库中的其余代码保持：

```text
unlabeled_candidate
```

不能全部标成负例。

---

# 十五、充分证据、完整证据和多组证书需要正式实现

建议新增：

```text
15_build_evidence_obligations.py
16_enumerate_sufficient_certificates.py
```

## 15.1 义务适用性

每条义务必须有：

```text
applicable
not_applicable
unknown
```

不能把无法判断的义务直接设成 optional。

## 15.2 Witness group

支持：

```text
any_sufficient
all_required
ordered_path
one_of_groups
```

例如：

```json
{
  "obligation": "dependency_context",
  "alternatives": [
    ["fault_unit", "caller_unit"],
    ["fault_unit", "test_constraint_unit"]
  ]
}
```

## 15.3 多组证书枚举

可以使用：

```text
DFS
分支定界
SAT
整数规划
加权集合覆盖
```

步骤：

```text
为每个 mandatory obligation 选择一个 witness group
→ 合并 Evidence Units
→ 检查结构约束
→ 删除冗余单元
→ 得到 inclusion-minimal certificate
→ 计算成本
→ 去重
```

## 15.4 不确定性记录

必须保存：

```text
certificate_status
verified / candidate / unresolved
enumeration_complete
truncated
max_certificates
unknown_obligations
```

---

# 十六、检索索引需要实现

建议新增：

```text
17_build_snapshot_retrieval_index.py
```

不能让模型扫描整个 Parquet。

需要建立：

```text
snapshot-aware BM25
snapshot-aware dense index
snapshot-aware structural graph
```

查询 API 必须是：

```python
search(
    snapshot_id,
    query,
    top_k
)
```

不能是：

```python
search(query, top_k)
```

即使使用一个全局向量数据库，也必须在 ANN 候选生成阶段过滤 `snapshot_id`。

## 16.1 两级检索

建议：

```text
第一级：
issue → files/modules

第二级：
issue + K_t → symbols/functions/blocks/tests
```

## 16.2 前端候选生成

参考 Nips：

```text
BM25 Top-k
+
Dense Top-k
→ RRF
→ 结构局部扩展
→ MMR
→ 10–30 个候选
```

## 16.3 FullRepo 评测

Retriever 的候选空间是完整 snapshot。

模型实际输入仍然只有：

```text
问题
+
当前状态
+
有限候选
```

---

# 十七、训练样本需要构建

建议新增：

```text
18_build_state_prefix_training_samples.py
```

样本结构：

```json
{
  "task_id": "...",
  "snapshot_id": "...",
  "t": 2,
  "question": "...",

  "state": {
    "selected_unit_ids": [],
    "K_t": "...",
    "covered_obligations": [],
    "missing_obligations": []
  },

  "candidates": {
    "C_t": []
  },

  "labels": {
    "acceptable_next_unit_ids": [],
    "candidate_roles": {},
    "deficit": {},
    "contribution": {},
    "interaction": {},
    "stop": {}
  }
}
```

## 17.1 Set-valued positive

由于存在多组充分证书，正例不能只有一个。

需要：

```text
acceptable_next_unit_ids
```

其含义是：

> 在当前状态下，加入其中任意一个候选都仍然可以通向某组有效充分证书。

## 17.2 固定候选池状态反事实

同一个任务不同状态应尽量保持候选池一致：

```text
同一 question
同一 C_t
不同 K_t
不同 missing obligation
不同 acceptable next evidence
```

这样才能证明模型真正使用状态，而不是从候选池变化猜测步骤。

---

# 十八、模型训练路线

## 18.1 Retriever

训练数据：

```text
SWE-bench patch old-side
ContextBench spans
SWE-Explore core
CORE-Bench qrels
完整仓库硬负例
```

初始模型：

```text
BM25
Dense bi-encoder
Hybrid
Cross-encoder reranker
```

## 18.2 Evidence Policy

输入：

```text
problem statement
current evidence state
candidate Evidence Unit
structure features
```

输出：

```text
ranking score
evidence role
missing obligation coverage
candidate contribution
complementarity
substitutability
```

## 18.3 State Encoder

不能无限拼接所有代码。

可以先采用：

```text
有界 compiled K_t
```

随后比较：

```text
query-only
previous evidence only
direct anchor
full accumulated text
set encoder
graph-aware state
obligation-aware compiled state
```

## 18.4 训练损失

可能包括：

[
L=
L_{\text{ranking}}
+\lambda_rL_{\text{role}}
+\lambda_dL_{\text{deficit}}
+\lambda_cL_{\text{contribution}}
+\lambda_iL_{\text{interaction}}
+\lambda_sL_{\text{stop}}
]

多组正例应采用 set-valued action loss：

[
L_{\text{set}}
==============

-\log
\frac{
\sum_{u\in A_t}\exp Q(u\mid K_t)
}{
\sum_{u\in C_t}\exp Q(u\mid K_t)
}
]

---

# 十九、训练数据量的解决方案

ContextBench 和 SWE-Explore 本身不足以从头训练大模型，但足以成为高质量锚点。

建议四层数据金字塔。

## Tier 1：强监督

```text
ContextBench verified
ContextBench gold
SWE-Explore core
多来源一致证据
```

## Tier 2：中等监督

```text
单来源 human/core
多轨迹共现
patch + structure
```

## Tier 3：弱监督

```text
全部 SWE-bench 类任务
patch old-side
test relation
call/import/def-use
结构候选
```

## Tier 4：自监督和反事实

```text
完整仓库硬负例
证据删除
顺序扰动
替代证据
同文件 near-miss
同符号错误单元
```

这样可以得到：

```text
约 20k 任务
× 每任务多个状态
× 每状态 10–100 候选
```

形成数十万到百万级 pairwise/ranking 样本。

任务独立性仍然只有约两万级，所以必须 repository-disjoint 评测，不能把 pair 数量当成独立任务数量。

---

# 二十、固定修复器行为监督尚未开始

这是后续成本最高但最关键的阶段。

建议新增：

```text
19_generate_controlled_evidence_packages.py
20_run_fixed_repairer_evaluations.py
21_compute_voi_interaction_labels.py
```

## 20.1 固定修复器

选择一个冻结的修复器 (G)。

所有实验保持：

```text
模型版本固定
prompt 固定
工具固定
温度固定
测试环境固定
最大 token 固定
修复尝试次数固定
```

## 20.2 受控证据包

对任务构建：

```text
K
K + u
K + v
K + {u,v}
```

运行固定修复器和真实测试。

## 20.3 计算标签

[
V_G(K)
]

[
\Delta_G(u\mid K)
]

[
I_G(u,v\mid K)
]

[
Sub_G(u\rightarrow v\mid K)
]

## 20.4 成本控制

不需要对所有 20k 任务和所有候选组合执行修复器。

建议：

```text
全量静态监督
+
1,000–3,000 个行为校准任务
+
重点互补/替代候选
+
高质量 dev/test 子集
```

---

# 二十一、大模型标注的边界

大模型只用于程序规则不能解决的语义歧义。

可以判断：

```text
两段代码是否表达同一约束
测试是否可以替代调用方
两个单元是否互补
文档和实现是否冲突
某条证据覆盖哪种 obligation
```

必须输出：

```text
source quotes
unit IDs
confidence
reason
```

程序随后验证：

```text
引用内容真实存在
unit 属于正确 snapshot
结构关系真实存在
没有 gold 泄漏
没有引用修复后代码
```

不能采用：

```text
LLM 生成 gold
→ 同一个 LLM 判断 gold
→ 作为真实标签
```

LLM 标签只能进入：

```text
weak supervision
semantic audit
unresolved adjudication
```

---

# 二十二、评测体系需要实现

## 22.1 Retriever 指标

```text
File Recall@k
Symbol Recall@k
Span Recall@B
MRR
NDCG
Supporting Unit Recall
Core Context Recall
```

## 22.2 证据指标

```text
Evidence Precision
Evidence Recall
Evidence F1
Mandatory Obligation Coverage
Structural Path Accuracy
Alternative Evidence Recall
```

## 22.3 证书指标

```text
Certificate Exact Match
Certificate Family Recall
Inclusion-Minimal Accuracy
Minimum-Cost Regret
Premature Certification Rate
False EXPAND Rate
ABSTAIN Accuracy
```

## 22.4 成本指标

```text
Evidence tokens
代码行数
Evidence Unit 数量
检索步数
tool calls
GPU latency
LLM/API cost
```

## 22.5 下游行为指标

```text
Resolved Rate
FAIL_TO_PASS
PASS_TO_PASS
Repair-Cost curve
AUC-Repair-Cost
```

---

# 二十三、基线和消融实验需要完成

## 23.1 检索基线

```text
BM25
Dense Retriever
BM25 + Dense
Cross-Encoder
SWE-bench BM25 context
Oracle context
Agentless localization
LocAgent graph retrieval
```

## 23.2 状态基线

参考 Nips：

```text
Query-only
Previous-evidence-only
Direct-anchor
Full accumulated state
Obligation-aware state
Set/graph state
```

必须复用：

```text
同一候选池
同一证据预算
同一模型容量
同一训练数据
同一随机种子
```

## 23.3 交互消融

```text
无 complementarity
无 substitutability
无结构特征
无 trajectory 特征
无人工上下文
无行为价值
无成本项
固定 Top-k
独立 STOP
VOI STOP
```

---

# 二十四、最终数据发布结构

最终仍建议压缩为三个核心文件：

```text
data/release_v2/
├── benchmark.jsonl
├── repository_corpus.parquet
└── manifest.json
```

## benchmark.jsonl

包含：

```text
任务输入
snapshot_id
source availability
support annotations
obligations
witness groups
certificate families
evaluation masks
```

offline labels 必须与 online input 明确隔离。

## repository_corpus.parquet

包含：

```text
content_unit
snapshot_unit
```

完整仓库代码只作为检索语料。

## manifest.json

包含：

```text
数据来源
版本
哈希
split
泄漏检查
完整性报告
证书统计
失败统计
排除策略
可见性规则
```

---

# 二十五、建议的实际执行顺序

## P0：恢复当前状态

```text
重新 inventory
验证 28M membership / 1.25M extraction
运行加速 extract
导出完整 corpus
```

完成门槛：

```text
repository_corpus.parquet 可用
snapshot 过滤验证通过
```

## P1：多源证据映射

```text
ContextBench
SWE-Explore
SWE-bench
→ logical_unit_id
```

完成门槛：

```text
span mapping 成功率
unmapped 原因统计
轨迹集合保留
```

## P2：程序结构图

```text
contains/imports/calls/reads/writes/raises/handles
```

完成门槛：

```text
所有边 snapshot-local
主要语言覆盖
结构路径可复现
```

## P3：支持证据与参考全集

```text
strong/support/weak/unresolved
reference_evidence_universe
```

完成门槛：

```text
不再 candidate=positive 塌缩
有真实 hard negatives
```

## P4：义务与证书

```text
applicability
mandatory
witness groups
certificate families
```

完成门槛：

```text
单单元证书比例显著下降
dependency/state/constraint 不再空洞
```

## P5：检索索引和候选生成

```text
snapshot BM25
snapshot Dense
structure expansion
MMR
```

完成门槛：

```text
FullRepo recall 可测
评测不 force-positive
```

## P6：状态训练样本

```text
certificate prefixes
set-valued positives
fixed candidate pools
```

完成门槛：

```text
有足够 t>=1 和 t>=2 样本
状态改变会改变最佳候选
```

## P7：模型训练

```text
Retriever
Reranker
State-aware evidence policy
Interaction-aware policy
```

完成门槛：

```text
相对 query-only 和 hybrid 有稳定提升
```

## P8：行为校准

```text
固定修复器
受控证据包
V/Delta/I/STOP
```

完成门槛：

```text
VOI 与真实修复结果有显著相关
```

## P9：认证与最终评测

```text
CERTIFIED
EXPAND
ABSTAIN
```

完成门槛：

```text
低 premature certification
高 obligation coverage
合理 evidence cost
```

## P10：正式 release 和论文

```text
数据说明
模型说明
泄漏审计
基线
消融
多种子
显著性检验
局限性
```

---

# 二十六、当前最需要避免的错误

1. 不要把完整仓库直接输入模型；
2. 不要全局检索后再过滤 snapshot；
3. 不要把 SWE-Explore optional 全部当正例；
4. 不要把未选中的候选全部当负例；
5. 不要只保存一组充分证书；
6. 不要把人工相关上下文称为绝对完整证据；
7. 不要把 patch 正确性和证据充分性混为一谈；
8. 不要让 gold patch、trajectory 或 labels 进入在线状态；
9. 不要在 FullRepo 评测时强制插入 gold；
10. 不要把 100 万 pair 当成 100 万独立任务；
11. 不要用同一个 LLM 生成并验证真值；
12. 不要使用 repo-only 过滤，必须使用 snapshot_id；
13. 不要让不同版本代码参与 BM25 IDF 或 ANN 候选竞争；
14. 不要继续使用当前 MVP 的自动证书作为正式充分性 gold；
15. 不要在状态库为空时继续 extract。

---

# 二十七、总体完成度判断

按研究工程拆分，可粗略估计：

| 模块                       | 当前状态           |
| ------------------------ | -------------- |
| 研究问题定义                   | 80%            |
| 多源数据规范化                  | 85%            |
| Master Registry          | 90%            |
| Split 与泄漏控制              | 75%            |
| Git 快照准备                 | 85%            |
| 候选级 Evidence Unit        | 90%            |
| MVP certification labels | 100%，但需替换      |
| MVP release              | 100%，仅调试用途     |
| 数据质量审计                   | 85%            |
| 完整仓库语料程序                 | 90%            |
| 完整仓库语料实际数据               | 约 0%，需恢复运行     |
| 多源证据统一                   | 20%，设计完成、代码未完成 |
| 程序结构图                    | 20%            |
| 支持证据正式标签                 | 15%            |
| 多组充分证书                   | 10%            |
| 检索索引                     | 5%             |
| 状态训练样本                   | 5%             |
| Retriever 训练             | 0%             |
| Evidence Policy 训练       | 0%             |
| 固定修复器行为监督                | 0%             |
| 证书校准与停止                  | 5%             |
| 正式主实验                    | 0%             |
| 最终论文数据                   | 10%            |

总体而言：

> 数据工程基础和研究框架已经搭好，但真正决定论文结论的完整仓库检索、多源支持证据、状态策略训练、行为价值和证书评测尚未完成。

当前最关键的单一任务不是继续设计更多标签，也不是立即训练模型，而是：

```text
先稳定生成完整、版本隔离的 repository_corpus.parquet
```

这是后续所有 FullRepo 检索、真实硬负例、状态训练样本和证书评测的共同基础。
